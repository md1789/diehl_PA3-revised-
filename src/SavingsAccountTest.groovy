import org.junit.Test
import static org.junit.Assert.assertEquals

class SavingsAccountTest extends groovy.util.GroovyTestCase {
    /*SavingsAccount saver1 = new SavingsAccount();
    SavingsAccount saver2 = new SavingsAccount();
    @Test
    void testMonthlySavings() {
        saver1.setSavingsBalance(2000.00);
        saver2.setSavingsBalance(3000.00);
        for (int i = 1; i <= 12; i++){
            saver1.savingsBalance = saver1.calculateMonthlyInterest();
            saver2.savingsBalance = saver2.calculateMonthlyInterest();
            System.out.println(saver1.savingsBalance);
            System.out.println(saver2.savingsBalance);
        }
    }

    @Test
    void testInterestRate() {
        double interestRate = 0.04;
        modifyInterestRate(interestRate);
        assertEquals(interestRate, getAnnualInterestRate(), 0.001);
    }*/
}
